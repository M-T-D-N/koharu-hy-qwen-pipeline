# Install into a project-owned virtual environment. Install a CUDA-enabled
# PyTorch build compatible with the host GPU separately before using Hy-MT2.
transformers==5.15.1
accelerate==1.14.0
huggingface-hub==1.27.0
